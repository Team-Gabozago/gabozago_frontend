const Title = ({ children }: { children: React.ReactNode }) => (
    <div className="text-silver text-title mt-8 mb-5 font-GangwonEduPower font-weight">
        {children}
    </div>
);

export default Title;
